source 'https://rubygems.org'

# Server requirements
gem 'thin' # or mongrel
# gem 'trinidad', platform: 'jruby'

# Project requirements
gem 'rack'
gem 'erubis'
gem 'dalli'
gem 'unidecoder', :git => "git://github.com/norman/unidecoder"
gem 'json', platform: 'ruby'

group :test do
  gem 'minitest'
  gem 'rack-test'
  gem 'rake'
  gem 'pry'
end
